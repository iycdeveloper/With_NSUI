import 'package:flutter/material.dart';

class MemberViewAppBar extends StatelessWidget {
  const MemberViewAppBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: [],
      ),
    );
  }
}
